ORYNTRA AI — MASTER TECHNICAL MANUAL
NARRATIVE ARCHITECTURE, OPERATIONS, AND FILE REFERENCE
Generated: 2026-08-27 23:18 UTC
Git source revision at generation: 4e68efc8fa5a9f133ee469ef1fa7f010c96197fa
Git branch: main

This manual is written for a reader who wants to understand how the checked-out Oryntra AI
system behaves, how its pieces relate, and where a change can create risk. It is not a source-
code mirror. It deliberately excludes raw code, private environment files, local databases,
cached market data, virtual environments, build products, and credentials. A documented source
file proves only what this checkout contains; it does not by itself prove a live server,
provider account, or database is current and healthy.

The document separates present-source facts from historical context. Oryntra's connected
Notion workspace contains valuable earlier NAS deployment and Pattern Lab material, but those
records refer to older snapshots. The current GitHub source reconciliation page should be read
before treating a past path, version, test count, or deployment claim as current.


HIGH-LEVEL FLOW

  Data providers / local cache
              |
              v
  Market repository -> validation + provenance -> deterministic analysis
              |                              |
              v                              v
  Private Quant Lab                    Derived scanner output
              |                              |
              v                              v
  risk/return diagnostics             browser workspace + explanations


========================================================================================
1. WHAT ORYNTRA IS AND IS NOT
========================================================================================
Oryntra is a market-intelligence and systematic-research product. It accepts a request for a
listed symbol or a group of symbols, obtains historical price data through server-side
provider adapters, derives technical and pattern observations, and returns a structured
interpretation to a browser or mobile client. The important word is structured: the product is
built around intermediate evidence such as indicators, detected patterns, score components,
confidence limits, source metadata, and scenario diagnostics. A conclusion is therefore
intended to be inspectable rather than presented as an unexplained prediction.

The project is not an order-management system, a brokerage connection, a live portfolio-
management service, or an investment adviser. The paper-trading features record simulated
actions inside the application, while the Quant Lab simulates fixed rules against daily
historical data. Neither system submits an order. That boundary is practical as well as legal:
it allows research and education to move faster than a regulated execution product while still
requiring the documentation to be candid about model limits.

The repository contains several generations of analytical engines and historical delivery
material. A reader must distinguish the current GitHub checkout from older NAS deployment
records and from experimental engine work. This manual describes the checked-out source.
Historical Notion pages are useful evidence of past decisions, but they are not treated as
confirmation of a current live deployment, database, provider account, or worker process.


========================================================================================
2. APPLICATION COMPOSITION AND REQUEST LIFECYCLE
========================================================================================
The application starts in server/run.py, which loads a private environment file when present
and launches Uvicorn. server/backend/main.py then becomes the composition root. It initializes
SQLite during the FastAPI lifespan, configures CORS and gzip, serves the browser files,
attaches policy-aware API routers, and publishes health and version information. The main
module also owns the distinction between a public derived-analysis site and private research
routes. That decision is made at startup from environment configuration rather than from
hidden client-side flags.

A typical browser scan is a staged pipeline. The route validates the request, asks the market
repository for a normalized history, calculates deterministic indicators, selects the
configured pattern and setup logic, derives an educational plan, optionally persists
noncritical history, and returns a JSON object. The public payload boundary then removes raw
market-data content that should not be exposed by the public product. The browser renders this
structured response; its AI explanation request is a separate step and does not replace the
numerical pipeline.

The product has a deliberately different lifecycle for compute-heavy research. Pattern Lab
jobs and Quant Lab runs are private capabilities. Pattern Lab can use persistent job state and
a separate worker so that long-running evaluations do not occupy the web server’s request
loop. Quant Lab performs a bounded synchronous research calculation in a worker thread after
histories have been obtained. The two systems share a research discipline—explicit data,
timing, costs, and diagnostics—but they solve different questions.


========================================================================================
3. DATA, PROVENANCE, AND CACHE CONTROL
========================================================================================
The market repository exists to prevent the rest of the application from caring which provider
supplied a candle. It normalizes ticker and period inputs, reads locally cached OHLCV history,
validates price and timestamp integrity, selects a configured provider when necessary, stores
usable bars, and returns both the frame and provenance metadata. The metadata records source,
provider, freshness, coverage, and fallback use. Quant Lab adds a fingerprint over its
selected histories and configuration so a completed run can be tied to the exact material it
evaluated.

Cache-first behavior is a reproducibility control. A live provider can revise data, impose a
rate limit, or fail in the middle of an experiment. When research uses a local cache, the user
can state what data were available for the run. The cache is durable state, not disposable
build output. Deployment procedures must preserve it alongside the application database, and
no documentation task should copy a populated database or credential file into a public
artifact.

Provider integration is intentionally server-side. Polygon and Twelve Data modules keep keys
in environment variables, limit calls, redact secrets in errors, and separate response
normalization from higher-level analysis. The application does not claim that the presence of
a provider adapter grants permission to redistribute vendor data. The browser receives derived
results under the configured policy boundary rather than an unrestricted raw-data API.


========================================================================================
4. ANALYTICAL ENGINE AND EXPLANATION BOUNDARY
========================================================================================
The deterministic core begins with indicators. The indicator module calculates common measures
such as moving averages, RSI, MACD, Bollinger Bands, ATR, ADX, volume measures, VWAP, Ichimoku
values, pivots, and trend labels from the supplied history. Pattern modules then evaluate
candle configurations, chart structures, fair-value gaps, and structure events. The pattern
analyzer coordinates broader engine generations, while the setup detector scores candidate
contexts such as breakout, pullback, continuation, reversal, overextended, and no-trade
states.

These components are not independent proof generators. A pattern detector labels observations
according to its rules; a setup scorer aggregates evidence according to its selected engine.
The trade scorer produces an educational plan-shaped object with risk/target calculations and
descriptive projections. The current public engine label is V7 Official Momentum. Other
engines, including V8 and VAI-based candidates, remain separate implementation paths that must
earn promotion through evidence rather than being silently substituted into the public
surface.

AI explanation is intentionally downstream. The explanation route takes structured analysis
and turns it into readable language through configured model providers or a rule-based
fallback. It does not calculate RSI, decide numeric pattern confidence, or receive a free-form
raw market-data feed as the primary source of truth. This is an important audit property: any
language output can be compared against the structured facts it is supposed to explain.


========================================================================================
5. QUANT LAB RESEARCH MODEL
========================================================================================
Quant Lab is the most explicit part of the repository about simulation mechanics. It supports
time-series trend, cross-sectional momentum, short-horizon mean reversion, and a defensive
low-volatility sleeve. Each sleeve converts prior daily price history into target weights. The
selected sleeves are combined according to visible allocations, then passed through the same
portfolio-control process. An ensemble is therefore not a hidden black box; it is a named
combination of documented component rules.

Execution timing is deliberately conservative for a daily-bar tool: a signal is formed with
the session close at time t, then the target is held for the following session. Weight changes
incur the user-entered transaction-cost assumption, and negative weights carry a user-entered
annual borrow-cost assumption. The controls cap individual names and gross exposure, rebalance
on the requested cadence, and use volatility targeting only to reduce exposure. They do not
manufacture leverage in order to make a backtest look smoother.

The report treats path and data quality as first-class outputs. It includes annualized return
and volatility, drawdown, turnover, historical VaR and expected shortfall, concentration,
current gross/net exposure, a chronological development-versus-holdout view, regime slices,
correlation matrix, monthly net-return heatmap, equity curve, drawdown curve, rolling
volatility, and source coverage. None of these eliminates model risk. They help identify
whether an attractive total return was obtained through concentration, a favorable regime,
sparse data, high turnover, or a brittle period of history.


========================================================================================
6. DURABLE STATE, ACCESS, AND PAPER RECORDS
========================================================================================
SQLite is the application’s durable memory. The database module initializes tables and exposes
helpers for accounts, sessions, subscriptions, watchlists, paper trades, analysis history,
market bars, pattern events, outcomes, cache metadata, and model-training records. The
database is deliberately treated as operational state. A code release that overwrites it can
destroy user records and reproducibility, so deployment and recovery documentation must keep
it outside ordinary source replacement.

Authentication uses server-side session handling, password hashing, active-subscription
checks, and request helpers that distinguish optional identity from a required signed-in user.
Analysis access adds an independent policy and quota layer. This allows the product to expose
a limited public derived-analysis experience while preserving owner and private-research
controls. It is not a substitute for a full enterprise identity, audit, or regulatory-
compliance system.

Paper-trading routes intentionally store simulated opening and closing records rather than
interact with a broker. They are useful for user workflow and for separating a user’s record
of a hypothetical action from a research report. They must not be described as evidence that a
strategy can be executed at the displayed price, size, or time in a live market.


========================================================================================
7. CLIENT, MOBILE, AND OPERATIONS
========================================================================================
The browser client is a conventional static workspace served by FastAPI. index.html
establishes semantic panels and controls; app.js owns state transitions, API calls, rendering,
accessibility behavior, and error/loading states; refined.css supplies the current restrained
blue research-desk presentation. The maintenance site is deliberately independent so a
maintenance response does not depend on the primary application rendering successfully. Legal
pages are static documents served through an explicit allowlist.

The ios-app directory is a Flutter client plus the iOS runner and widget integration. The
mobile application should be read as a separate client surface, not as a duplicate of the
Python service. Its API service, session storage, screens, consent/ad services, and widgets
rely on the backend contract. Native Swift and Xcode configuration files bridge Flutter into
iOS and host the widget extension. Mobile changes need their own simulator/device and App
Store validation; a successful Python test does not validate iOS behaviour.

Operational tools exist for cache inspection and warming, research execution, bias auditing,
training, database-related safeguards, and local launch. These tools are intentionally
explicit rather than magical. They should be run from the server directory with the correct
environment, and destructive deployment actions must preserve credentials, data, generated
models, training history, and the virtual environment. A service that listens on a port is not
automatically healthy; check the real health response and the intended user flow.


========================================================================================
8. REPOSITORY COVERAGE AND READING MAP
========================================================================================
Included files: 155
  - Automated regression test: 11
  - Backend service or research engine: 27
  - Browser application structure: 1
  - Browser presentation or client behaviour: 4
  - Deterministic pattern analysis: 8
  - Flutter mobile application: 24
  - HTTP API route: 13
  - Legal and policy surface: 6
  - Maintenance-mode application: 8
  - Mobile application configuration or operator asset: 9
  - Native iOS integration or build configuration: 22
  - Operator or research utility: 12
  - Product and operating model: 1
  - Server configuration or operator asset: 9

The catalogue that follows is intentionally file-by-file, but it is not a reproduction of the
code. Each entry identifies the file's real role in the system, explains the boundaries it
owns, and notes the operational or product implication that matters when it changes. The
descriptions are densest for modules that determine data provenance, analytical logic, access
boundaries, durable state, or client contracts; small configuration and platform files are
described by the particular build or runtime contract they carry.

INCLUDED FILES

  - README.md (264 lines; Product and operating model)
  - ios-app/analysis_options.yaml (6 lines; Mobile application configuration or operator asset)
  - ios-app/build_ipa.sh (16 lines; Mobile application configuration or operator asset)
  - ios-app/configure_admob.sh (39 lines; Mobile application configuration or operator asset)
  - ios-app/configure_widget_target.rb (66 lines; Mobile application configuration or operator asset)
  - ios-app/ios/Flutter/AppFrameworkInfo.plist (17 lines; Native iOS integration or build configuration)
  - ios-app/ios/Flutter/Debug.xcconfig (3 lines; Native iOS integration or build configuration)
  - ios-app/ios/Flutter/Flutter.podspec (19 lines; Native iOS integration or build configuration)
  - ios-app/ios/Flutter/Release.xcconfig (3 lines; Native iOS integration or build configuration)
  - ios-app/ios/OryntraWidget/Info.plist (28 lines; Native iOS integration or build configuration)
  - ios-app/ios/OryntraWidget/OryntraWidget.entitlements (11 lines; Native iOS integration or build configuration)
  - ios-app/ios/OryntraWidget/OryntraWidget.swift (110 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner.xcodeproj/project.pbxproj (603 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner.xcodeproj/project.xcworkspace/contents.xcworkspacedata (3 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner.xcodeproj/xcshareddata/xcschemes/Runner.xcscheme (88 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner.xcworkspace/contents.xcworkspacedata (11 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/AppDelegate.swift (324 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json (24 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Assets.xcassets/Contents.json (2 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Assets.xcassets/LaunchImage.imageset/Contents.json (9 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Base.lproj/LaunchScreen.storyboard (30 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Base.lproj/Main.storyboard (20 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/GeneratedPluginRegistrant.h (20 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/GeneratedPluginRegistrant.m (43 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Info.plist (81 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Runner-Bridging-Header.h (2 lines; Native iOS integration or build configuration)
  - ios-app/ios/Runner/Runner.entitlements (17 lines; Native iOS integration or build configuration)
  - ios-app/lib/app_config.dart (71 lines; Flutter mobile application)
  - ios-app/lib/main.dart (258 lines; Flutter mobile application)
  - ios-app/lib/screens/account_screen.dart (693 lines; Flutter mobile application)
  - ios-app/lib/screens/paper_screen.dart (242 lines; Flutter mobile application)
  - ios-app/lib/screens/scanner_screen.dart (1393 lines; Flutter mobile application)
  - ios-app/lib/screens/watchlist_screen.dart (277 lines; Flutter mobile application)
  - ios-app/lib/services/api_service.dart (553 lines; Flutter mobile application)
  - ios-app/lib/services/consent_service.dart (3 lines; Flutter mobile application)
  - ios-app/lib/services/consent_service_mobile.dart (53 lines; Flutter mobile application)
  - ios-app/lib/services/consent_service_web.dart (8 lines; Flutter mobile application)
  - ios-app/lib/services/interstitial_ad_service.dart (3 lines; Flutter mobile application)
  - ios-app/lib/services/interstitial_ad_service_mobile.dart (75 lines; Flutter mobile application)
  - ios-app/lib/services/interstitial_ad_service_web.dart (8 lines; Flutter mobile application)
  - ios-app/lib/services/notification_service.dart (216 lines; Flutter mobile application)
  - ios-app/lib/services/session_store.dart (36 lines; Flutter mobile application)
  - ios-app/lib/services/widget_service.dart (27 lines; Flutter mobile application)
  - ios-app/lib/widgets/adaptive_banner.dart (3 lines; Flutter mobile application)
  - ios-app/lib/widgets/adaptive_banner_mobile.dart (72 lines; Flutter mobile application)
  - ios-app/lib/widgets/adaptive_banner_web.dart (24 lines; Flutter mobile application)
  - ios-app/lib/widgets/common.dart (107 lines; Flutter mobile application)
  - ios-app/lib/widgets/glass.dart (84 lines; Flutter mobile application)
  - ios-app/lib/widgets/tradingview_chart.dart (3 lines; Flutter mobile application)
  - ios-app/lib/widgets/tradingview_chart_mobile.dart (148 lines; Flutter mobile application)
  - ios-app/lib/widgets/tradingview_chart_web.dart (47 lines; Flutter mobile application)
  - ios-app/prepare_ios_project.sh (27 lines; Mobile application configuration or operator asset)
  - ios-app/pubspec.yaml (29 lines; Mobile application configuration or operator asset)
  - ios-app/web/flutter_bootstrap.js (12 lines; Mobile application configuration or operator asset)
  - ios-app/web/index.html (55 lines; Mobile application configuration or operator asset)
  - ios-app/web/manifest.json (23 lines; Mobile application configuration or operator asset)
  - server/BACKFILL_MARKET_DATA_MAC.command (12 lines; Server configuration or operator asset)
  - server/CHECK_MARKET_CACHE_MAC.command (11 lines; Server configuration or operator asset)
  - server/QUANT_LAB.md (42 lines; Server configuration or operator asset)
  - server/START_MAINTENANCE_MODE.command (5 lines; Server configuration or operator asset)
  - server/START_MAINTENANCE_MODE.sh (5 lines; Server configuration or operator asset)
  - server/START_ORYNTRA_MAC.command (28 lines; Server configuration or operator asset)
  - server/backend/__init__.py (2 lines; Backend service or research engine)
  - server/backend/analysis_access.py (192 lines; Backend service or research engine)
  - server/backend/backtest.py (525 lines; Backend service or research engine)
  - server/backend/database.py (1329 lines; Backend service or research engine)
  - server/backend/documented_beta_counts.py (29 lines; Backend service or research engine)
  - server/backend/fetcher.py (54 lines; Backend service or research engine)
  - server/backend/indicators.py (480 lines; Backend service or research engine)
  - server/backend/lab_grading.py (64 lines; Backend service or research engine)
  - server/backend/main.py (317 lines; Backend service or research engine)
  - server/backend/market_cache.py (559 lines; Backend service or research engine)
  - server/backend/market_repository.py (839 lines; Backend service or research engine)
  - server/backend/pattern_analyzer.py (1171 lines; Backend service or research engine)
  - server/backend/pattern_lab.py (1099 lines; Backend service or research engine)
  - server/backend/pattern_lab_jobs.py (312 lines; Backend service or research engine)
  - server/backend/pattern_lab_worker.py (150 lines; Backend service or research engine)
  - server/backend/patterns/__init__.py (5 lines; Deterministic pattern analysis)
  - server/backend/patterns/candle_patterns.py (347 lines; Deterministic pattern analysis)
  - server/backend/patterns/chart_patterns.py (249 lines; Deterministic pattern analysis)
  - server/backend/patterns/fair_value_gaps.py (147 lines; Deterministic pattern analysis)
  - server/backend/patterns/outcome_tracker.py (58 lines; Deterministic pattern analysis)
  - server/backend/patterns/pattern_engine.py (348 lines; Deterministic pattern analysis)
  - server/backend/patterns/structure_patterns.py (128 lines; Deterministic pattern analysis)
  - server/backend/patterns/utils.py (128 lines; Deterministic pattern analysis)
  - server/backend/polygon_client.py (185 lines; Backend service or research engine)
  - server/backend/public_payload.py (119 lines; Backend service or research engine)
  - server/backend/quant_research.py (234 lines; Backend service or research engine)
  - server/backend/research_experiments.py (338 lines; Backend service or research engine)
  - server/backend/research_training.py (175 lines; Backend service or research engine)
  - server/backend/research_universe.py (111 lines; Backend service or research engine)
  - server/backend/routes/__init__.py (2 lines; HTTP API route)
  - server/backend/routes/ai_explain.py (236 lines; HTTP API route)
  - server/backend/routes/analysis.py (403 lines; HTTP API route)
  - server/backend/routes/auth.py (337 lines; HTTP API route)
  - server/backend/routes/backtest.py (25 lines; HTTP API route)
  - server/backend/routes/dev_tools.py (420 lines; HTTP API route)
  - server/backend/routes/intelligence.py (145 lines; HTTP API route)
  - server/backend/routes/paper_trading.py (188 lines; HTTP API route)
  - server/backend/routes/patterns.py (31 lines; HTTP API route)
  - server/backend/routes/pro.py (1145 lines; HTTP API route)
  - server/backend/routes/pro_live.py (471 lines; HTTP API route)
  - server/backend/routes/quant.py (114 lines; HTTP API route)
  - server/backend/routes/watchlist.py (50 lines; HTTP API route)
  - server/backend/setup_detector.py (1313 lines; Backend service or research engine)
  - server/backend/trade_scorer.py (515 lines; Backend service or research engine)
  - server/backend/twelvedata_client.py (155 lines; Backend service or research engine)
  - server/backend/v8_engine.py (468 lines; Backend service or research engine)
  - server/backend/vai2_model.py (621 lines; Backend service or research engine)
  - server/backend/vai_model.py (373 lines; Backend service or research engine)
  - server/frontend/index.html (168 lines; Browser application structure)
  - server/frontend/legal/contact.html (11 lines; Legal and policy surface)
  - server/frontend/legal/methodology.html (16 lines; Legal and policy surface)
  - server/frontend/legal/privacy.html (16 lines; Legal and policy surface)
  - server/frontend/legal/refund.html (12 lines; Legal and policy surface)
  - server/frontend/legal/risk-disclaimer.html (15 lines; Legal and policy surface)
  - server/frontend/legal/terms.html (17 lines; Legal and policy surface)
  - server/frontend/static/css/main.css (3679 lines; Browser presentation or client behaviour)
  - server/frontend/static/css/refined.css (543 lines; Browser presentation or client behaviour)
  - server/frontend/static/js/app.js (3292 lines; Browser presentation or client behaviour)
  - server/frontend/static/js/theme-init.js (26 lines; Browser presentation or client behaviour)
  - server/maintenance_site/app.py (136 lines; Maintenance-mode application)
  - server/maintenance_site/index.html (76 lines; Maintenance-mode application)
  - server/maintenance_site/legal/methodology.html (405 lines; Maintenance-mode application)
  - server/maintenance_site/static/css/main.css (3673 lines; Maintenance-mode application)
  - server/maintenance_site/static/css/maintenance.css (375 lines; Maintenance-mode application)
  - server/maintenance_site/static/css/refined.css (2257 lines; Maintenance-mode application)
  - server/maintenance_site/static/js/maintenance.js (9 lines; Maintenance-mode application)
  - server/maintenance_site/static/js/theme-init.js (26 lines; Maintenance-mode application)
  - server/requirements.txt (12 lines; Server configuration or operator asset)
  - server/run.py (54 lines; Server configuration or operator asset)
  - server/shared/ad_slots.json (49 lines; Server configuration or operator asset)
  - server/tests/conftest.py (10 lines; Automated regression test)
  - server/tests/test_analysis_access_policy.py (51 lines; Automated regression test)
  - server/tests/test_dev_tools_jobs.py (118 lines; Automated regression test)
  - server/tests/test_intelligence_route.py (68 lines; Automated regression test)
  - server/tests/test_market_cache.py (98 lines; Automated regression test)
  - server/tests/test_market_repository.py (133 lines; Automated regression test)
  - server/tests/test_pattern_lab_next.py (49 lines; Automated regression test)
  - server/tests/test_public_payload_boundary.py (62 lines; Automated regression test)
  - server/tests/test_quant_research.py (31 lines; Automated regression test)
  - server/tests/test_research_pipeline.py (150 lines; Automated regression test)
  - server/tests/test_v8_engine.py (100 lines; Automated regression test)
  - server/tools/__init__.py (2 lines; Operator or research utility)
  - server/tools/audit_v7_bias.py (244 lines; Operator or research utility)
  - server/tools/cache_guard.py (95 lines; Operator or research utility)
  - server/tools/check_cache_status.py (9 lines; Operator or research utility)
  - server/tools/generate_master_docs.py (704 lines; Operator or research utility)
  - server/tools/market_cache_cli.py (92 lines; Operator or research utility)
  - server/tools/run_backtest_cli.py (119 lines; Operator or research utility)
  - server/tools/run_pattern_lab_cli.py (101 lines; Operator or research utility)
  - server/tools/seed_app_review_account.py (56 lines; Operator or research utility)
  - server/tools/train_vai2_cli.py (120 lines; Operator or research utility)
  - server/tools/vai_full_train_pipeline.py (41 lines; Operator or research utility)
  - server/tools/warm_cache_cli.py (59 lines; Operator or research utility)

========================================================================================
9. ENVIRONMENT VARIABLE REFERENCE
========================================================================================
The following names were found in safe source/configuration files. Values are intentionally
omitted. Keep populated environment files and service credentials out of source control and
out of public documentation.

  - ADMOB_ANDROID_APP_ID
  - ADMOB_IOS_APP_ID
  - ADSENSE_AUTO_ADS_ENABLED
  - ADSENSE_CLIENT_ID
  - ADSENSE_HEAD
  - ADSENSE_PUBLISHER_ID
  - ADSENSE_SITE_URL
  - ADSENSE_SLOT
  - ADSENSE_VERIFY_ENABLED
  - APP_REVIEW_EMAIL
  - APP_REVIEW_PASSWORD
  - ORYNTRA_ALLOW_MANUAL_BETA_SUBSCRIPTIONS
  - ORYNTRA_ANALYSIS_CACHE_MAX_ITEMS
  - ORYNTRA_ANALYSIS_RESULT_TTL
  - ORYNTRA_API_URL
  - ORYNTRA_CACHE_MAX_STALE_DAYS
  - ORYNTRA_CORS_ORIGINS
  - ORYNTRA_DAILY_ANALYSIS_LIMIT
  - ORYNTRA_DB_PATH
  - ORYNTRA_ENABLE_TWELVEDATA_FALLBACK
  - ORYNTRA_ENABLE_YFINANCE_FALLBACK
  - ORYNTRA_HTTP_TIMEOUT
  - ORYNTRA_LOG_LEVEL
  - ORYNTRA_MAINTENANCE_HOST
  - ORYNTRA_MAINTENANCE_PORT
  - ORYNTRA_MARKET_CACHE_
  - ORYNTRA_MARKET_CACHE_AUTO_BACKFILL
  - ORYNTRA_MARKET_CACHE_AUTO_START
  - ORYNTRA_MARKET_CACHE_EOD_READY_HOUR_ET
  - ORYNTRA_MARKET_CACHE_HTTP_TIMEOUT
  - ORYNTRA_MARKET_CACHE_INITIAL_DELAY_SECONDS
  - ORYNTRA_MARKET_CACHE_LOOKBACK_DAYS
  - ORYNTRA_MARKET_CACHE_MAX_CONSECUTIVE_FAILURES
  - ORYNTRA_MARKET_CACHE_MIN_ROWS
  - ORYNTRA_MARKET_CACHE_POLL_SECONDS
  - ORYNTRA_MARKET_CACHE_REFERENCE_MAX_PAGES
  - ORYNTRA_MARKET_CACHE_REFERENCE_REFRESH_DAYS
  - ORYNTRA_MARKET_CACHE_RETENTION_DAYS
  - ORYNTRA_MARKET_CACHE_SYNC_REFERENCE
  - ORYNTRA_MARKET_CACHE_UPDATE_SESSIONS
  - ORYNTRA_MARKET_DATA_LICENSE_MODE
  - ORYNTRA_NEGATIVE_CACHE_HOURS
  - ORYNTRA_OWNER_EMAILS
  - ORYNTRA_PATTERN_LAB_CHECKPOINT_TICKERS
  - ORYNTRA_PATTERN_LAB_CPU_SHARE
  - ORYNTRA_PATTERN_LAB_JOB_ID
  - ORYNTRA_PATTERN_LAB_MAX_BOOTSTRAP
  - ORYNTRA_PATTERN_LAB_MAX_RETURNED_ROWS
  - ORYNTRA_PATTERN_LAB_NICE
  - ORYNTRA_PATTERN_LOOKBACK_BARS
  - ORYNTRA_PLAN_DISPLAY_PRICE_USD
  - ORYNTRA_POLYGON_CALLS_PER_MINUTE
  - ORYNTRA_POLYGON_MAX_RETRIES
  - ORYNTRA_POLYGON_MIN_GAP
  - ORYNTRA_PREVIEW_MODE
  - ORYNTRA_PRIVATE_RESEARCH_ROUTES
  - ORYNTRA_PRO_ALERTS_AUTO_START
  - ORYNTRA_PRO_ALERTS_INTERVAL_SECONDS
  - ORYNTRA_PRO_LIVE_CACHE_SECONDS
  - ORYNTRA_PRO_QUOTE_CACHE_MINUTES
  - ORYNTRA_PUBLIC_DERIVED_ANALYSIS_ENABLED
  - ORYNTRA_PUBLIC_SCANNER_WEBSITE
  - ORYNTRA_RELOAD
  - ORYNTRA_RESEARCH_MAX_UNIVERSE
  - ORYNTRA_SCAN_CONCURRENCY
  - ORYNTRA_SCAN_LOOKBACK_BARS
  - ORYNTRA_SUBSCRIPTIONS_ENFORCED
  - ORYNTRA_TWELVEDATA_CALLS_PER_MINUTE
  - ORYNTRA_TWELVEDATA_MAX_RETRIES
  - ORYNTRA_TWELVEDATA_MIN_GAP
  - POLYGON_API_KEY
  - POLYGON_BASE_URL
  - PORT
  - PUBLIC_ANALYSIS_DISABLED
  - PUBLIC_BASE_URL
  - PUBLIC_ENGINE_LABEL
  - TWELVEDATA_API_KEY
  - TWELVEDATA_BASE_URL
  - TWELVE_DATA_BASE_URL
  - WEB_ADS_ENABLED


========================================================================================
10. FILE-BY-FILE NARRATIVE REFERENCE
========================================================================================

[README.md]
Classification: Product and operating model. Source size: 264 lines.

The repository README is the public orientation document. It explains Oryntra's research
scope, the major product surfaces, local setup, and the intended limits of the system. It
should stay aligned with the actual release label and should never imply that a research or
paper-trading feature executes securities transactions.


[ios-app/analysis_options.yaml]
Classification: Mobile application configuration or operator asset. Source size: 6 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/build_ipa.sh]
Classification: Mobile application configuration or operator asset. Source size: 16 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/configure_admob.sh]
Classification: Mobile application configuration or operator asset. Source size: 39 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/configure_widget_target.rb]
Classification: Mobile application configuration or operator asset. Source size: 66 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Flutter/AppFrameworkInfo.plist]
Classification: Native iOS integration or build configuration. Source size: 17 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Flutter/Debug.xcconfig]
Classification: Native iOS integration or build configuration. Source size: 3 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Flutter/Flutter.podspec]
Classification: Native iOS integration or build configuration. Source size: 19 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Flutter/Release.xcconfig]
Classification: Native iOS integration or build configuration. Source size: 3 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/OryntraWidget/Info.plist]
Classification: Native iOS integration or build configuration. Source size: 28 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/OryntraWidget/OryntraWidget.entitlements]
Classification: Native iOS integration or build configuration. Source size: 11 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/OryntraWidget/OryntraWidget.swift]
Classification: Native iOS integration or build configuration. Source size: 110 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner.xcodeproj/project.pbxproj]
Classification: Native iOS integration or build configuration. Source size: 603 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner.xcodeproj/project.xcworkspace/contents.xcworkspacedata]
Classification: Native iOS integration or build configuration. Source size: 3 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner.xcodeproj/xcshareddata/xcschemes/Runner.xcscheme]
Classification: Native iOS integration or build configuration. Source size: 88 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner.xcworkspace/contents.xcworkspacedata]
Classification: Native iOS integration or build configuration. Source size: 11 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/AppDelegate.swift]
Classification: Native iOS integration or build configuration. Source size: 324 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json]
Classification: Native iOS integration or build configuration. Source size: 24 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Assets.xcassets/Contents.json]
Classification: Native iOS integration or build configuration. Source size: 2 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Assets.xcassets/LaunchImage.imageset/Contents.json]
Classification: Native iOS integration or build configuration. Source size: 9 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Base.lproj/LaunchScreen.storyboard]
Classification: Native iOS integration or build configuration. Source size: 30 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Base.lproj/Main.storyboard]
Classification: Native iOS integration or build configuration. Source size: 20 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/GeneratedPluginRegistrant.h]
Classification: Native iOS integration or build configuration. Source size: 20 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/GeneratedPluginRegistrant.m]
Classification: Native iOS integration or build configuration. Source size: 43 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Info.plist]
Classification: Native iOS integration or build configuration. Source size: 81 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Runner-Bridging-Header.h]
Classification: Native iOS integration or build configuration. Source size: 2 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/ios/Runner/Runner.entitlements]
Classification: Native iOS integration or build configuration. Source size: 17 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/lib/app_config.dart]
Classification: Flutter mobile application. Source size: 71 lines.

This file centralizes Flutter application configuration, including the server endpoint
selection used by the mobile client. Keeping that decision in one place prevents individual
screens from inventing incompatible URLs or release assumptions.


[ios-app/lib/main.dart]
Classification: Flutter mobile application. Source size: 258 lines.

This is the Flutter client’s application shell. It composes navigation and high-level page
state while coordinating backend access through the client services. It should be validated as
a mobile application rather than assumed correct because the web client works.


[ios-app/lib/screens/account_screen.dart]
Classification: Flutter mobile application. Source size: 693 lines.

This Flutter screen presents identity, subscription, and account-state presentation. It should
treat server responses as untrusted remote state, preserving loading, empty, error, and
signed-out behavior rather than assuming that a successful desktop interaction proves the
mobile flow is complete.


[ios-app/lib/screens/paper_screen.dart]
Classification: Flutter mobile application. Source size: 242 lines.

This Flutter screen presents simulated-trade records and paper-performance presentation. It
should treat server responses as untrusted remote state, preserving loading, empty, error, and
signed-out behavior rather than assuming that a successful desktop interaction proves the
mobile flow is complete.


[ios-app/lib/screens/scanner_screen.dart]
Classification: Flutter mobile application. Source size: 1393 lines.

This Flutter screen presents symbol analysis, scanner results, and their loading/error states.
It should treat server responses as untrusted remote state, preserving loading, empty, error,
and signed-out behavior rather than assuming that a successful desktop interaction proves the
mobile flow is complete.


[ios-app/lib/screens/watchlist_screen.dart]
Classification: Flutter mobile application. Source size: 277 lines.

This Flutter screen presents saved-symbol management and its account-scoped empty states. It
should treat server responses as untrusted remote state, preserving loading, empty, error, and
signed-out behavior rather than assuming that a successful desktop interaction proves the
mobile flow is complete.


[ios-app/lib/services/api_service.dart]
Classification: Flutter mobile application. Source size: 553 lines.

This service owns the Flutter-side HTTP contract. It defines how the mobile client reads,
transforms, and handles Oryntra API responses. Contract changes on the Python side should be
checked here before an iOS release.


[ios-app/lib/services/consent_service.dart]
Classification: Flutter mobile application. Source size: 3 lines.

This service implements the platform-neutral consent interface. Keeping this work behind a
service boundary lets screens remain focused on presentation and makes platform-specific
behavior explicit; any change needs both a Flutter check and the relevant device or browser
test.


[ios-app/lib/services/consent_service_mobile.dart]
Classification: Flutter mobile application. Source size: 53 lines.

This service implements mobile-platform consent behavior. Keeping this work behind a service
boundary lets screens remain focused on presentation and makes platform-specific behavior
explicit; any change needs both a Flutter check and the relevant device or browser test.


[ios-app/lib/services/consent_service_web.dart]
Classification: Flutter mobile application. Source size: 8 lines.

This service implements web-platform consent behavior. Keeping this work behind a service
boundary lets screens remain focused on presentation and makes platform-specific behavior
explicit; any change needs both a Flutter check and the relevant device or browser test.


[ios-app/lib/services/interstitial_ad_service.dart]
Classification: Flutter mobile application. Source size: 3 lines.

This service implements the platform-neutral interstitial-ad interface. Keeping this work
behind a service boundary lets screens remain focused on presentation and makes platform-
specific behavior explicit; any change needs both a Flutter check and the relevant device or
browser test.


[ios-app/lib/services/interstitial_ad_service_mobile.dart]
Classification: Flutter mobile application. Source size: 75 lines.

This service implements mobile interstitial-ad lifecycle behavior. Keeping this work behind a
service boundary lets screens remain focused on presentation and makes platform-specific
behavior explicit; any change needs both a Flutter check and the relevant device or browser
test.


[ios-app/lib/services/interstitial_ad_service_web.dart]
Classification: Flutter mobile application. Source size: 8 lines.

This service implements the no-native-ad web implementation. Keeping this work behind a
service boundary lets screens remain focused on presentation and makes platform-specific
behavior explicit; any change needs both a Flutter check and the relevant device or browser
test.


[ios-app/lib/services/notification_service.dart]
Classification: Flutter mobile application. Source size: 216 lines.

This service implements local notification scheduling and user permission handling. Keeping
this work behind a service boundary lets screens remain focused on presentation and makes
platform-specific behavior explicit; any change needs both a Flutter check and the relevant
device or browser test.


[ios-app/lib/services/session_store.dart]
Classification: Flutter mobile application. Source size: 36 lines.

This service implements secure or persistent mobile session storage. Keeping this work behind
a service boundary lets screens remain focused on presentation and makes platform-specific
behavior explicit; any change needs both a Flutter check and the relevant device or browser
test.


[ios-app/lib/services/widget_service.dart]
Classification: Flutter mobile application. Source size: 27 lines.

This service implements communication between the Flutter client and the iOS widget extension.
Keeping this work behind a service boundary lets screens remain focused on presentation and
makes platform-specific behavior explicit; any change needs both a Flutter check and the
relevant device or browser test.


[ios-app/lib/widgets/adaptive_banner.dart]
Classification: Flutter mobile application. Source size: 3 lines.

This Flutter widget provides the platform-neutral banner-ad widget contract. Its success is
visual and behavioral: it must preserve layout, accessibility, lifecycle safety, and the
correct empty or unavailable state on the platforms where it is used.


[ios-app/lib/widgets/adaptive_banner_mobile.dart]
Classification: Flutter mobile application. Source size: 72 lines.

This Flutter widget provides the mobile banner-ad implementation. Its success is visual and
behavioral: it must preserve layout, accessibility, lifecycle safety, and the correct empty or
unavailable state on the platforms where it is used.


[ios-app/lib/widgets/adaptive_banner_web.dart]
Classification: Flutter mobile application. Source size: 24 lines.

This Flutter widget provides the web fallback or web-specific banner treatment. Its success is
visual and behavioral: it must preserve layout, accessibility, lifecycle safety, and the
correct empty or unavailable state on the platforms where it is used.


[ios-app/lib/widgets/common.dart]
Classification: Flutter mobile application. Source size: 107 lines.

This Flutter widget provides shared visual primitives used by more than one screen. Its
success is visual and behavioral: it must preserve layout, accessibility, lifecycle safety,
and the correct empty or unavailable state on the platforms where it is used.


[ios-app/lib/widgets/glass.dart]
Classification: Flutter mobile application. Source size: 84 lines.

This Flutter widget provides the reusable translucent visual treatment used by the mobile
interface. Its success is visual and behavioral: it must preserve layout, accessibility,
lifecycle safety, and the correct empty or unavailable state on the platforms where it is
used.


[ios-app/lib/widgets/tradingview_chart.dart]
Classification: Flutter mobile application. Source size: 3 lines.

This Flutter widget provides the platform-neutral chart widget contract. Its success is visual
and behavioral: it must preserve layout, accessibility, lifecycle safety, and the correct
empty or unavailable state on the platforms where it is used.


[ios-app/lib/widgets/tradingview_chart_mobile.dart]
Classification: Flutter mobile application. Source size: 148 lines.

This Flutter widget provides the mobile chart embedding implementation. Its success is visual
and behavioral: it must preserve layout, accessibility, lifecycle safety, and the correct
empty or unavailable state on the platforms where it is used.


[ios-app/lib/widgets/tradingview_chart_web.dart]
Classification: Flutter mobile application. Source size: 47 lines.

This Flutter widget provides the web chart embedding implementation. Its success is visual and
behavioral: it must preserve layout, accessibility, lifecycle safety, and the correct empty or
unavailable state on the platforms where it is used.


[ios-app/prepare_ios_project.sh]
Classification: Mobile application configuration or operator asset. Source size: 27 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/pubspec.yaml]
Classification: Mobile application configuration or operator asset. Source size: 29 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/web/flutter_bootstrap.js]
Classification: Mobile application configuration or operator asset. Source size: 12 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/web/index.html]
Classification: Mobile application configuration or operator asset. Source size: 55 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[ios-app/web/manifest.json]
Classification: Mobile application configuration or operator asset. Source size: 23 lines.

This mobile-support file contributes to the Flutter or iOS client boundary. Read it with the
screen, service, widget, or native target that consumes it, because a local source change can
fail later at archive time or on a real device.


[server/BACKFILL_MARKET_DATA_MAC.command]
Classification: Server configuration or operator asset. Source size: 12 lines.

Its operational sequence begins with set -euo pipefail, cd "$(dirname "$0")", if [ ! -x
".venv-mac/bin/python" ]; then, echo "Run START_ORYNTRA_MAC.command once first so the Mac
environment is installed.", exit 1, fi, source .venv-mac/bin/activate, python
tools/market_cache_cli.py backfill. Shell and project-configuration helpers should be read as
procedures with side effects; validate their target paths and environment before executing
them on any persistent installation.


[server/CHECK_MARKET_CACHE_MAC.command]
Classification: Server configuration or operator asset. Source size: 11 lines.

Its operational sequence begins with set -euo pipefail, cd "$(dirname "$0")", if [ -x ".venv-
mac/bin/python" ]; then, source .venv-mac/bin/activate, python tools/market_cache_cli.py
status, else, python3 tools/market_cache_cli.py status, fi. Shell and project-configuration
helpers should be read as procedures with side effects; validate their target paths and
environment before executing them on any persistent installation.


[server/QUANT_LAB.md]
Classification: Server configuration or operator asset. Source size: 42 lines.

Its substantive sections are Oryntra Quant Lab, What a run evaluates, Data sources, Research
standard. This document is part of the product's operating contract: revise it when the
corresponding user-visible or operational behaviour changes, not merely to make it sound more
complete.


[server/START_MAINTENANCE_MODE.command]
Classification: Server configuration or operator asset. Source size: 5 lines.

Its operational sequence begins with set -e, cd "$(dirname "$0")", exec python3
maintenance_site/app.py --host 0.0.0.0 --port "${ORYNTRA_MAINTENANCE_PORT:-8000}". Shell and
project-configuration helpers should be read as procedures with side effects; validate their
target paths and environment before executing them on any persistent installation.


[server/START_MAINTENANCE_MODE.sh]
Classification: Server configuration or operator asset. Source size: 5 lines.

Its operational sequence begins with set -euo pipefail, cd "$(dirname "$0")", exec python3
maintenance_site/app.py --host 0.0.0.0 --port "${ORYNTRA_MAINTENANCE_PORT:-8000}". Shell and
project-configuration helpers should be read as procedures with side effects; validate their
target paths and environment before executing them on any persistent installation.


[server/START_ORYNTRA_MAC.command]
Classification: Server configuration or operator asset. Source size: 28 lines.

Its operational sequence begins with set -euo pipefail, cd "$(dirname "$0")",
PYTHON_BIN="${PYTHON_BIN:-python3}", VENV_DIR=".venv-mac", if [ ! -x "$VENV_DIR/bin/python" ];
then, echo "Creating Mac Python environment...", "$PYTHON_BIN" -m venv "$VENV_DIR", fi, and
additional private helpers. Shell and project-configuration helpers should be read as
procedures with side effects; validate their target paths and environment before executing
them on any persistent installation.


[server/backend/__init__.py]
Classification: Backend service or research engine. Source size: 2 lines.

This package initializer establishes the backend namespace. It is intentionally small;
meaningful application behavior lives in the modules that it makes importable.


[server/backend/analysis_access.py]
Classification: Backend service or research engine. Source size: 192 lines.

This module centralizes whether a request may use an analysis capability and whether it
consumes a quota. Keeping that decision server-side prevents the browser from becoming the
authority for access policy and makes public, signed-in, owner, and private research modes
explicit.


[server/backend/backtest.py]
Classification: Backend service or research engine. Source size: 525 lines.

This module supplies the lighter historical backtest path used outside the full Quant Lab. It
is useful for quick analytical context, but it should not be confused with the broader
portfolio simulation, data-fingerprint, or validation diagnostic framework implemented in
quant_research.py.

Its declared HTTP boundary includes POST /run.


[server/backend/database.py]
Classification: Backend service or research engine. Source size: 1329 lines.

This is the durable-state boundary. It owns table setup and persistence operations for the
application’s account, trade, analysis, cache, and research histories. It should be changed
with a migration mindset: inspect existing data, preserve the database file, and test
initialization against a nonproduction copy before deploying.


[server/backend/documented_beta_counts.py]
Classification: Backend service or research engine. Source size: 29 lines.

This module computes the small public-facing counts displayed in the beta product. It
deliberately separates a presentation statistic from private research or operational data, so
changes should preserve that distinction and avoid exposing raw histories or account records.


[server/backend/fetcher.py]
Classification: Backend service or research engine. Source size: 54 lines.

This provider-facing adapter acquires historical market data for the ordinary analysis
pipeline. Its responsibility is to make a usable, normalized history available to the rest of
the backend while leaving provenance, cache policy, and provider fallback governed by the
repository layer.


[server/backend/indicators.py]
Classification: Backend service or research engine. Source size: 480 lines.

This module transforms an OHLCV history into a normalized technical state. It deliberately
centralizes numerical calculations so setup, pattern, plan, and explanation layers receive the
same inputs. If an indicator convention changes here, every consumer can change behaviour, so
regression tests and a representative scan comparison are appropriate.


[server/backend/lab_grading.py]
Classification: Backend service or research engine. Source size: 64 lines.

This module grades Pattern Lab output against explicit research criteria. It gives the product
a structured way to report whether an experiment is sufficiently complete or internally
consistent, while stopping short of treating a grade as proof of future profitability.


[server/backend/main.py]
Classification: Backend service or research engine. Source size: 317 lines.

This is the server's composition root. It decides which routers exist in a given operating
mode, protects frontend responses with no-cache headers, serves legal and static assets, and
exposes health/version/ads diagnostics. A change here can alter the public attack surface or
make a private research feature visible, so changes should be followed by a startup and route-
boundary check rather than a syntax check alone.

Its declared HTTP boundary includes GET /, GET /legal/{page_name}, GET /health, GET
/api/app/version, GET /api/app/stats, GET /api/app/market-cache, GET /api/app/ads, GET
/api/app/ads/diagnostics, GET /ads.txt, GET /robots.txt, GET /sitemap.xml.


[server/backend/market_cache.py]
Classification: Backend service or research engine. Source size: 559 lines.

This module manages the trading-calendar-aware cache lifecycle: planned backfill dates,
grouped-day imports, reference-symbol synchronization, retention, and an optional worker. It
handles operations rather than user-facing analysis, so its primary concerns are data
completeness, rate discipline, recoverability, and avoiding unnecessary provider load.


[server/backend/market_repository.py]
Classification: Backend service or research engine. Source size: 839 lines.

This module is the canonical price-history abstraction. Its job is not merely to download
bars: it validates symbol format, controls provider preference, normalizes cache and provider
payloads into one frame shape, records source metadata, and calculates the reproducibility
fingerprint used by research. Any provider change belongs here before downstream engines are
allowed to rely on it.


[server/backend/pattern_analyzer.py]
Classification: Backend service or research engine. Source size: 1171 lines.

This module is the switchboard for pattern-engine generations. It retains legacy and candidate
behavior while defining the official V7 path and later V8/VAI-related experimental paths. Its
job is to compose a comparable report from lower-level pattern evidence, not to make an
unqualified profitability claim.


[server/backend/pattern_lab.py]
Classification: Backend service or research engine. Source size: 1099 lines.

Pattern Lab evaluates engine observations across a universe and historical windows. It builds
causal observations, simulates target/stop outcomes under costs, creates baselines, groups
results by relevant context, and reports walk-forward and bootstrap diagnostics. It is a
research harness, not a production model-promotion authority.


[server/backend/pattern_lab_jobs.py]
Classification: Backend service or research engine. Source size: 312 lines.

This module makes long Pattern Lab work resumable. It writes requests, status, checkpoints,
logs, and results atomically; launches or terminates a separate worker; and provides enough
filesystem state to report progress after a web request ends. Its file layout is operational
state and should be preserved during a deployment.


[server/backend/pattern_lab_worker.py]
Classification: Backend service or research engine. Source size: 150 lines.

This is the isolated process entry point for Pattern Lab. It configures resource use, restores
request/checkpoint state, runs the evaluation, writes terminal status, and turns worker
exceptions into inspectable job results. Its role is to protect the normal web request path
from CPU-heavy research work.


[server/backend/patterns/__init__.py]
Classification: Deterministic pattern analysis. Source size: 5 lines.

This package initializer establishes the deterministic pattern-analysis namespace. It is
supporting structure for the detector modules rather than a separate analytical engine.


[server/backend/patterns/candle_patterns.py]
Classification: Deterministic pattern analysis. Source size: 347 lines.

This detector identifies named candlestick configurations from bar relationships. It produces
labels and supporting measurements for the wider pattern report; the labels are deterministic
descriptions of the implemented rules rather than guarantees about the next price movement.


[server/backend/patterns/chart_patterns.py]
Classification: Deterministic pattern analysis. Source size: 249 lines.

This detector looks for broader price-structure formations such as consolidations, breakouts,
and reversals. Its output feeds the pattern engine and should be compared on fixed histories
after any threshold or lookback change because detection frequency can move significantly.


[server/backend/patterns/fair_value_gaps.py]
Classification: Deterministic pattern analysis. Source size: 147 lines.

This module identifies gap-like price structures using the project's defined bar
relationships. The resulting zones are analytical annotations, not executable liquidity
levels, and they must remain tied to the timestamp and data source that produced them.


[server/backend/patterns/outcome_tracker.py]
Classification: Deterministic pattern analysis. Source size: 58 lines.

This module evaluates what followed a recorded pattern observation. It is central to Pattern
Lab's causal framing because outcomes must be assessed after a signal is formed, with stated
cost and timing assumptions, rather than inferred from a chart inspected in hindsight.


[server/backend/patterns/pattern_engine.py]
Classification: Deterministic pattern analysis. Source size: 348 lines.

This module coordinates the lower-level pattern detectors into a single evidence object. It is
the integration point where a consumer receives coherent pattern context instead of having to
combine candle, chart, structure, and gap outputs independently.


[server/backend/patterns/structure_patterns.py]
Classification: Deterministic pattern analysis. Source size: 128 lines.

This detector describes market-structure events from successive swings and price
relationships. Its role is to supply contextual evidence to the pattern engine; it does not
establish a trading instruction by itself.


[server/backend/patterns/utils.py]
Classification: Deterministic pattern analysis. Source size: 128 lines.

This file contains shared numerical and data-shape helpers used across pattern detectors. It
is a high-leverage dependency: a small change can alter multiple pattern families, so fixed-
example regression checks are more informative than inspecting one visual chart.


[server/backend/polygon_client.py]
Classification: Backend service or research engine. Source size: 185 lines.

This adapter contains the Polygon-specific request and response handling. It is kept separate
from analysis logic so provider quirks, credentials, rate limits, and payload normalization do
not leak into indicators or user-facing route code.


[server/backend/public_payload.py]
Classification: Backend service or research engine. Source size: 119 lines.

This module is the public-data boundary. It reduces internal analysis objects to the derived
information that the public product is allowed to return and strips fields that would
otherwise create an uncontrolled raw-market-data surface.


[server/backend/quant_research.py]
Classification: Backend service or research engine. Source size: 234 lines.

This module is the Quant Lab calculation engine. It builds each transparent signal sleeve,
applies weight and volatility controls, simulates next-session returns after modeled costs,
and produces the risk, validation, data-quality, and visual-diagnostic objects rendered by the
Quant Desk. It is deliberately daily-bar research code; it should not be extended with
execution claims without new data, modeling, and policy work.


[server/backend/research_experiments.py]
Classification: Backend service or research engine. Source size: 338 lines.

This module holds reusable experimental helpers for research comparisons. It belongs to the
evidence-producing side of the system and should be read alongside its datasets, assumptions,
and tests rather than promoted into the public scanner merely because it produces a favorable
result.


[server/backend/research_training.py]
Classification: Backend service or research engine. Source size: 175 lines.

This module prepares and evaluates training-oriented research work. It connects stored
observations to candidate-model workflows while preserving a distinction between a trained
artifact and an approved production decision rule.


[server/backend/research_universe.py]
Classification: Backend service or research engine. Source size: 111 lines.

This module defines research symbol universes and related selection controls. Universe design
materially affects reported results, so a run must identify whether it used the intended
membership and should not imply survivorship-free coverage unless the data actually provides
it.


[server/backend/routes/__init__.py]
Classification: HTTP API route. Source size: 2 lines.

This package initializer marks the route collection as an importable backend boundary. It
carries little standalone behavior but provides the namespace that the main application uses
when composing routers.


[server/backend/routes/ai_explain.py]
Classification: HTTP API route. Source size: 236 lines.

This route produces a readable explanation from structured analysis. It keeps language
generation downstream of deterministic calculations and provides a controlled fallback path,
which makes it possible to compare prose with the numerical evidence that supports it.

Its declared HTTP boundary includes POST /explain, POST /explain-indicator.


[server/backend/routes/analysis.py]
Classification: HTTP API route. Source size: 403 lines.

This route module controls the ordinary market-scan lifecycle. It coalesces equivalent
requests, uses a bounded result cache, runs blocking calculations away from the event loop,
separates response-critical work from deferred persistence, and provides single-symbol,
watchlist, screening, comparison, statistics, and history surfaces. Its response schema is
central to browser compatibility.

Its declared HTTP boundary includes POST /scan, POST /scan-multiple, POST /screener, POST
/compare, GET /stats, GET /history/{ticker}.


[server/backend/routes/auth.py]
Classification: HTTP API route. Source size: 337 lines.

This route implements account and session flows for the browser client. It validates
authentication requests and delegates durable identity work to the database layer; cookie,
session, and authorization changes here should be tested as full browser flows rather than
only as JSON responses.

Its declared HTTP boundary includes POST /signup, POST /login, POST /logout, GET /me, DELETE
/account, POST /subscribe.


[server/backend/routes/backtest.py]
Classification: HTTP API route. Source size: 25 lines.

This route exposes the quick historical backtest capability. It is a thin HTTP boundary around
the backtest helper, so validation should focus on request limits, clean errors, and making
its simplified assumptions distinct from a full Quant Lab report.

Its declared HTTP boundary includes POST /run, GET /quick/{ticker}.


[server/backend/routes/dev_tools.py]
Classification: HTTP API route. Source size: 420 lines.

This private route group controls operational and research workflows such as cache warming,
model status, VAI training, and Pattern Lab jobs. It is intentionally not a normal public
product surface; authorization and long-running-job behavior are more important here than a
polished consumer response.

Its declared HTTP boundary includes GET /pattern-modes, GET /pattern-lab/universe, POST
/counters/sync-beta-counts, GET /cache/status, POST /cache/warm-start, GET /cache/warm-
status/{job_id}, GET /vai/model/status, GET /vai2/model/status, POST /vai/train/start, GET
/vai/train/status/{job_id}, POST /pattern-lab/run, POST /pattern-lab/start, among other
private operations.


[server/backend/routes/intelligence.py]
Classification: HTTP API route. Source size: 145 lines.

This route group serves the derived intelligence scan and its quota/status information. It is
the controlled bridge between scanner analysis and the public payload policy, including the
multiple-symbol path that must remain bounded under concurrent use.

Its declared HTTP boundary includes GET /status, GET /quota, POST /scan, POST /scan-multiple.


[server/backend/routes/paper_trading.py]
Classification: HTTP API route. Source size: 188 lines.

This route group records and retrieves simulated trades and paper statistics. It preserves an
account-level historical record for educational use but does not obtain market fills, transmit
orders, or verify live execution conditions.

Its declared HTTP boundary includes GET /trades, GET /trades/all, POST /open, POST /close, GET
/stats.


[server/backend/routes/patterns.py]
Classification: HTTP API route. Source size: 31 lines.

This route presents deterministic pattern-analysis results over the API. It should retain a
clear separation between descriptive pattern evidence and any later explanation or plan-shaped
output shown by the client.

Its declared HTTP boundary includes GET /events, GET /stats.


[server/backend/routes/pro.py]
Classification: HTTP API route. Source size: 1145 lines.

This route contains subscription-aware or professional workspace endpoints. It is an access
boundary as well as a response layer, so its authorization checks must remain server-enforced
even if the client hides or reveals the corresponding screen.

Its declared HTTP boundary includes GET /snapshot/{ticker}, GET /live/{ticker}, GET
/chart/{ticker}, GET /cache/{ticker}, GET /alerts, POST /alerts, PATCH /alerts/{alert_id},
DELETE /alerts/{alert_id}, GET /alert-events, POST /alerts/evaluate/{ticker}, GET
/paper/trades, POST /paper/open, among other private operations.


[server/backend/routes/pro_live.py]
Classification: HTTP API route. Source size: 471 lines.

This route provides the protected live-oriented professional surface. The name does not turn
the product into a brokerage or order system; it still needs data-source provenance, access
enforcement, and careful product language.


[server/backend/routes/quant.py]
Classification: HTTP API route. Source size: 114 lines.

This route module forms the private Quant Lab API contract. Pydantic validation constrains
ticker count, period, provider, model, costs, leverage-related limits, and rebalancing choice
before histories are fetched. It then returns the simulator report together with
configuration, provider metadata, errors, timestamps, and a fingerprint so that the UI does
not have to infer what ran.

Its declared HTTP boundary includes GET /catalog, POST /run.


[server/backend/routes/watchlist.py]
Classification: HTTP API route. Source size: 50 lines.

This route group persists and returns user watchlist selections. It couples account identity
with durable state, so tests should cover ownership and empty-state behavior as well as normal
create, list, and removal actions.

Its declared HTTP boundary includes GET /, POST /add, DELETE /{ticker}.


[server/backend/setup_detector.py]
Classification: Backend service or research engine. Source size: 1313 lines.

This module converts indicator and pattern context into named setup candidates. The successive
adjustment functions preserve the history of engine generations, including V7 official
adjustments and V8 candidate logic. Because this is a scoring layer, even a small threshold
change can affect public classifications and must be validated across more than one ticker and
date range.


[server/backend/trade_scorer.py]
Classification: Backend service or research engine. Source size: 515 lines.

This module converts a selected setup into an educational trade-plan-shaped object. It
calculates plan levels, quality/conviction labels, sizing suggestions, and projections from
previously derived facts. The output must remain framed as educational analysis because it is
not connected to real execution, liquidity, or user suitability information.


[server/backend/twelvedata_client.py]
Classification: Backend service or research engine. Source size: 155 lines.

This provider adapter implements the private Twelve Data integration with conservative request
pacing and response normalization. It belongs beside the other server-side provider clients
rather than in the browser so keys, vendor limits, failure details, and licensing boundaries
remain controlled by the backend.


[server/backend/v8_engine.py]
Classification: Backend service or research engine. Source size: 468 lines.

This module contains the symmetric candidate-scoring building blocks used for V8-oriented
research. It scores directional alignment by factors such as trend, momentum, volatility, and
pattern confirmation. It is separate from the publicly labeled V7 engine so research
comparisons do not silently change ordinary scan behaviour.


[server/backend/vai2_model.py]
Classification: Backend service or research engine. Source size: 621 lines.

This later learned-model path adds training-quality metrics, promotion comparison, and richer
feature processing. It still belongs to the research side of the application: it can produce
candidate or promoted artifacts inside the project, but its evaluation must remain separate
from a claim that the public scanner is using an institutionally validated model.


[server/backend/vai_model.py]
Classification: Backend service or research engine. Source size: 373 lines.

This is an earlier learned-model path built from Pattern Lab rows. It assembles numeric and
categorical features, fits simple model components, writes candidate artifacts, and returns a
prediction-shaped result for comparison. The presence of model code does not make the
prediction deployable without separate evidence, model governance, and held-out validation.


[server/frontend/index.html]
Classification: Browser application structure. Source size: 168 lines.

This document is the browser application’s structural contract. Its element IDs, tab
containers, forms, dialogs, and data attributes are consumed by app.js. Removing apparently
cosmetic markup can break client initialization, so interface changes should preserve or
intentionally migrate those hooks.


[server/frontend/legal/contact.html]
Classification: Legal and policy surface. Source size: 11 lines.

This contact document is a policy-facing page served by the product. Its statements must
remain consistent with actual data, account, subscription, advertising, and research behavior;
legal wording should not be changed merely as interface copy.


[server/frontend/legal/methodology.html]
Classification: Legal and policy surface. Source size: 16 lines.

This methodology document is a policy-facing page served by the product. Its statements must
remain consistent with actual data, account, subscription, advertising, and research behavior;
legal wording should not be changed merely as interface copy.


[server/frontend/legal/privacy.html]
Classification: Legal and policy surface. Source size: 16 lines.

This privacy document is a policy-facing page served by the product. Its statements must
remain consistent with actual data, account, subscription, advertising, and research behavior;
legal wording should not be changed merely as interface copy.


[server/frontend/legal/refund.html]
Classification: Legal and policy surface. Source size: 12 lines.

This refund document is a policy-facing page served by the product. Its statements must remain
consistent with actual data, account, subscription, advertising, and research behavior; legal
wording should not be changed merely as interface copy.


[server/frontend/legal/risk-disclaimer.html]
Classification: Legal and policy surface. Source size: 15 lines.

This risk disclaimer document is a policy-facing page served by the product. Its statements
must remain consistent with actual data, account, subscription, advertising, and research
behavior; legal wording should not be changed merely as interface copy.


[server/frontend/legal/terms.html]
Classification: Legal and policy surface. Source size: 17 lines.

This terms document is a policy-facing page served by the product. Its statements must remain
consistent with actual data, account, subscription, advertising, and research behavior; legal
wording should not be changed merely as interface copy.


[server/frontend/static/css/main.css]
Classification: Browser presentation or client behaviour. Source size: 3679 lines.

This stylesheet contains the older or shared browser presentation rules. It needs to coexist
predictably with refined.css; when a visual issue appears, determine which layer owns the
selector instead of adding an override that obscures the active design system.


[server/frontend/static/css/refined.css]
Classification: Browser presentation or client behaviour. Source size: 543 lines.

This stylesheet is the current blue research-desk presentation layer. It defines the visual
hierarchy and responsive behavior for the shared workspace and Quant Desk. Its selectors must
continue to match the actual HTML and JavaScript state classes; visual review at desktop and
mobile widths is the appropriate validation.


[server/frontend/static/js/app.js]
Classification: Browser presentation or client behaviour. Source size: 3292 lines.

This is the browser orchestrator. It owns client-side API calls, account transitions, tab
selection, scans, results, charts, paper records, settings, and Quant Desk rendering. It
contains legacy assumptions and multiple startup paths, so browser-console checks after a
clean reload are especially important when the HTML changes.


[server/frontend/static/js/theme-init.js]
Classification: Browser presentation or client behaviour. Source size: 26 lines.

This small startup script applies the persisted visual theme before the main client has
rendered. Its main purpose is to avoid an unnecessary flash of the wrong appearance and to
keep user preference behavior independent of the larger application bundle.


[server/maintenance_site/app.py]
Classification: Maintenance-mode application. Source size: 136 lines.

This is a deliberately separate, minimal maintenance-mode server. It gives operators a
controlled response surface when the primary application should not be presented, and
therefore must avoid depending on normal scanner, database, or frontend startup behavior.


[server/maintenance_site/index.html]
Classification: Maintenance-mode application. Source size: 76 lines.

This is the maintenance-mode landing document. It communicates temporary availability status
without relying on the primary web application's selectors, client bundle, or API routes.


[server/maintenance_site/legal/methodology.html]
Classification: Maintenance-mode application. Source size: 405 lines.

This methodology document is a policy-facing page served by the product. Its statements must
remain consistent with actual data, account, subscription, advertising, and research behavior;
legal wording should not be changed merely as interface copy.


[server/maintenance_site/static/css/main.css]
Classification: Maintenance-mode application. Source size: 3673 lines.

This stylesheet supports the independent maintenance-mode presentation. It should remain
deliberately smaller and more failure-tolerant than the primary workspace, because the point
of maintenance mode is to work when ordinary application rendering is unavailable.


[server/maintenance_site/static/css/maintenance.css]
Classification: Maintenance-mode application. Source size: 375 lines.

This stylesheet supports the independent maintenance-mode presentation. It should remain
deliberately smaller and more failure-tolerant than the primary workspace, because the point
of maintenance mode is to work when ordinary application rendering is unavailable.


[server/maintenance_site/static/css/refined.css]
Classification: Maintenance-mode application. Source size: 2257 lines.

This stylesheet supports the independent maintenance-mode presentation. It should remain
deliberately smaller and more failure-tolerant than the primary workspace, because the point
of maintenance mode is to work when ordinary application rendering is unavailable.


[server/maintenance_site/static/js/maintenance.js]
Classification: Maintenance-mode application. Source size: 9 lines.

This client script supports the small maintenance-mode experience. Its limited scope is
intentional: an outage or operator-mode page should have fewer moving parts than the normal
workspace.


[server/maintenance_site/static/js/theme-init.js]
Classification: Maintenance-mode application. Source size: 26 lines.

This is the maintenance site's early theme initializer. It maintains a consistent visual
preference without importing the main application's larger stateful browser code.


[server/requirements.txt]
Classification: Server configuration or operator asset. Source size: 12 lines.

This dependency manifest defines the Python packages required to run the backend and its
research features. Version changes can affect numerical results, provider behavior, and
deployment reproducibility, so dependency updates deserve their own validation rather than
being treated as housekeeping.


[server/run.py]
Classification: Server configuration or operator asset. Source size: 54 lines.

This is the local server launcher. It loads environment configuration when available and
starts the ASGI service, making it the narrow handoff from a developer's shell to the FastAPI
composition root. A successful process start is only the beginning of validation; the health
response and intended client flow still need to be exercised.


[server/shared/ad_slots.json]
Classification: Server configuration or operator asset. Source size: 49 lines.

This configuration defines named advertising placement metadata separate from templates and
application logic. It allows the server to reason about enabled slots without hard-coding
placement decisions throughout the frontend.


[server/tests/conftest.py]
Classification: Automated regression test. Source size: 10 lines.

This test support file prepares shared fixtures and import behavior for the backend suite. It
is part of the testing environment itself, so a change can affect many tests even when no
product module has changed.


[server/tests/test_analysis_access_policy.py]
Classification: Automated regression test. Source size: 51 lines.

This regression suite verifies analysis-access and quota policy. Its value is that it tests a
product boundary the browser must not be able to bypass.


[server/tests/test_dev_tools_jobs.py]
Classification: Automated regression test. Source size: 118 lines.

This suite exercises the private job-control behavior used by development and research tools.
It guards against job-state regressions that can otherwise appear only after a long-running
task starts or resumes.


[server/tests/test_intelligence_route.py]
Classification: Automated regression test. Source size: 68 lines.

This suite verifies the public intelligence route's request and response behavior. It protects
the derived scanner contract, including error handling and bounded multi-symbol work.


[server/tests/test_market_cache.py]
Classification: Automated regression test. Source size: 98 lines.

This suite verifies cache calendar, freshness, and update behavior. It protects
reproducibility and provider discipline rather than only testing that a request can fetch data
once.


[server/tests/test_market_repository.py]
Classification: Automated regression test. Source size: 133 lines.

This suite exercises the provenance-carrying market repository. It is the main regression
check for provider normalization, cache preference, validation, and the metadata that later
research reports rely on.


[server/tests/test_pattern_lab_next.py]
Classification: Automated regression test. Source size: 49 lines.

This suite checks the newer Pattern Lab evaluation path. It helps ensure that historical
outcomes are generated causally and that experiment diagnostics remain present after
implementation changes.


[server/tests/test_public_payload_boundary.py]
Classification: Automated regression test. Source size: 62 lines.

This suite verifies the public-payload reduction policy. It is a security and product test: a
successful internal analysis must not automatically mean that every underlying field is
public.


[server/tests/test_quant_research.py]
Classification: Automated regression test. Source size: 31 lines.

This suite checks core Quant Lab mechanics and report outputs. It should be expanded whenever
a sleeve, timing rule, cost treatment, or risk diagnostic changes because aggregate
performance alone is not a sufficient regression signal.


[server/tests/test_research_pipeline.py]
Classification: Automated regression test. Source size: 150 lines.

This suite covers the handoff among research preparation, candidate-model work, and the
surrounding evaluation pipeline. It protects the evidence workflow from becoming disconnected
after a refactor.


[server/tests/test_v8_engine.py]
Classification: Automated regression test. Source size: 100 lines.

This suite checks the V8 candidate-scoring path separately from the public V7 path. That
separation matters because experimental comparisons must not silently change the labeled
public engine.


[server/tools/__init__.py]
Classification: Operator or research utility. Source size: 2 lines.

This package initializer establishes the tools namespace. The individual commands contain the
operator behavior; this file merely supports their import structure.


[server/tools/audit_v7_bias.py]
Classification: Operator or research utility. Source size: 244 lines.

This command-line research tool audits V7 behavior for obvious bias or evaluation issues. It
is a diagnostic aid, not a certification process, and its output must be interpreted with the
exact universe, dates, and assumptions it used.


[server/tools/cache_guard.py]
Classification: Operator or research utility. Source size: 95 lines.

This operational utility inspects cache size and supports guarded backup or restore behavior.
It exists to reduce the risk of silently replacing a useful local market-data history during
maintenance.


[server/tools/check_cache_status.py]
Classification: Operator or research utility. Source size: 9 lines.

This small wrapper reports the current cache condition through the shared guard logic. It is
intended for an operator who needs a quick evidence check before a data refresh or deployment.


[server/tools/generate_master_docs.py]
Classification: Operator or research utility. Source size: 704 lines.

This generator produces this manual from the checked-out source without reading private
environment values, local databases, cached histories, or generated build products. Its
responsibility is to keep the file reference synchronized with the repository while explicitly
favoring narrative explanation over pasted source text.


[server/tools/market_cache_cli.py]
Classification: Operator or research utility. Source size: 92 lines.

This is the command-line interface for cache inspection, synchronization, and backfill
operations. It is an operator tool with durable-data consequences, so it should be pointed
only at a known environment and run with a documented provider configuration.


[server/tools/run_backtest_cli.py]
Classification: Operator or research utility. Source size: 119 lines.

This utility runs the lighter backtest workflow from the command line. It is useful for
reproducible local checks, provided the input period, symbols, source state, and simplified
assumptions are recorded with the result.


[server/tools/run_pattern_lab_cli.py]
Classification: Operator or research utility. Source size: 101 lines.

This utility invokes Pattern Lab from a terminal rather than through the browser. It is useful
for repeatable research runs, but long executions and outputs should still be treated as job
state rather than transient console text.


[server/tools/seed_app_review_account.py]
Classification: Operator or research utility. Source size: 56 lines.

This operator helper prepares a controlled account state for mobile review or testing. It
should be used only in the intended database environment and never be mistaken for a general
production-account administration path.


[server/tools/train_vai2_cli.py]
Classification: Operator or research utility. Source size: 120 lines.

This utility starts the VAI2 candidate-model training workflow from the command line. It
produces research artifacts whose inputs, metrics, and promotion criteria must be retained
with the experiment.


[server/tools/vai_full_train_pipeline.py]
Classification: Operator or research utility. Source size: 41 lines.

This command-line pipeline coordinates the longer end-to-end VAI training sequence. It is an
experiment runner, not a live prediction service, and it should preserve outputs and failure
evidence for later review.


[server/tools/warm_cache_cli.py]
Classification: Operator or research utility. Source size: 59 lines.

This utility warms historical-data cache coverage before research or scanner work. Its purpose
is to make provider use more predictable and to improve reproducibility, not to guarantee that
data are complete or vendor-correct.


========================================================================================
11. TEST, RELEASE, AND REVIEW DISCIPLINE
========================================================================================
Run tests from `server/` so imports resolve from the application root: `PYTHONPATH=.
.venv/bin/python3 -m unittest discover -s tests -v`. For a focused Quant Lab check, run
`PYTHONPATH=. .venv/bin/python3 -m unittest tests.test_quant_research`. Validate the browser
client with `node --check frontend/static/js/app.js`. A release candidate should also be
started locally and checked through the actual `/health` endpoint and intended access modes.

The manual should be regenerated when source architecture changes, and the generated commit
identifier should be recorded with any environment-specific verification. Do not treat this
document as legal, investment, tax, security, or market-data licensing advice. For a
production release, pair this manual with live verification of health, the configured route
boundary, provider behavior, database preservation, and the actual client flows that users
will encounter.

